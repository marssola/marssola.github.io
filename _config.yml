# Site settings

title: Mauro Marssola
description: [C++/Qt and Linux Developer Lover]
baseurl: ''
url: 'https://marssola.github.io'
github_username: marssola
license_url: https://raw.githubusercontent.com/marssola/marssola.github.io/main/LICENSE

# Build settings
markdown: kramdown

plugins:
  - jekyll-seo-tag

# use 'post' layout for posts by default (so it can be omitted in front matter)
defaults:
  -
    scope:
      path: '' # an empty string here means all files in the project
      type: 'posts' # previously `post` in Jekyll 2.2.
    values:
      layout: 'post'